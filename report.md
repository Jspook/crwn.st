# รายงานสาเหตุปัญหา (Root Cause Analysis) ประจำวันที่ 2

## 1. สถานะของห้องลองในหน้าของพนักงาน (ห้อง 1, 2 ปล่อยแล้วไม่ว่าง / ลูกค้าเข้าไม่ได้)
- **สาเหตุ:** การจัดการ Session และสถานะห้องมีความขัดแย้งกัน ใน `GET /customer/fitting-room` เมื่อห้องว่าง (available) และมีคนสแกนเข้า ระบบจะเปลี่ยนสถานะเป็น occupied ทันที และเมื่อพนักงาน "ปล่อยห้องว่าง" ผ่าน `POST /api/fitting-rooms/:num/release` ระบบเปลี่ยน `FTR_Status` เป็น 'available' ให้ห้องจริง แต่ในตาราง `FITTING_SESSION` ยังมี session ค้างเป็นของคนเก่าล่าสุดอยู่ ทำให้เมื่อลูกค้ารายใหม่พยายามจะเข้าห้อง (`POST /api/fitting-sessions`) ระบบไปตรวจสอบ `lastSession.CUS_ID` ที่เป็นของคนเก่าและดักไว้ว่าห้องยังใช้ไม่ได้ แม้สถานะห้องจะว่างแล้วก็ตาม

## 2. ออเดอร์ใช้เวลานานหรือไม่ขึ้นเลยในหน้าพนักงาน (ต้องรีเฟรช)
- **สาเหตุ:** แม้ในฝั่งพนักงานจะมี `setInterval()` ในการดึงข้อมูล แต่เนื่องจากตัวแปร `roomSig` และ `lastKanbanSignature` อาจถูกจัดการไม่ถูกต้อง หรือไม่สะท้อนการเปลี่ยนแปลงย่อย ทำให้ไม่เข้าสู่เงื่อนไข `renderKanban()` เว้นแต่พนักงานจะกดรีเฟรชเอง (Force Render) นอกจากนี้ ในฝั่งลูกค้า เมื่อส่งคำขอ `POST /api/fitting-orders` จะมีการใช้ Toast แจ้งเตือนสั้นๆ แล้วปิดไป ทำให้ลูกค้าไม่เห็นสถานะชัดเจน

## 3. ไม่ขึ้น popup ถามให้เลือกลงตะกร้าเมื่อกดออกจากห้องลอง (ถ้าสถานะเป็น "ส่งแล้ว")
- **สาเหตุ:** ในฟังก์ชัน `confirmExitFittingRoom()` หน้า Fitting Room ระบบจะ fetch หา order ทั้งหมดใน session เพื่อนำมาให้เลือกใส่ตะกร้า แต่จะเช็คว่า `if (sessionTriedItems.length === 0)` ให้ exit โดยไม่ต้องถาม ในขณะเดียวกัน API `GET /api/fitting-orders` อาจจะกรอง order เฉพาะบางสถานะ หรือเมื่อออเดอร์มี status เป็น `complete` ("ส่งแล้ว") ไม่ได้ถูกนับรวมใน list ที่ดึงมาอย่างถูกต้อง ทำให้ตัวแปร length เป็น 0 แล้วจบการทำงานไปเลย

## 4. จำนวนตะกร้าไม่อัปเดตเมื่อเข้าหน้าห้องลอง
- **สาเหตุ:** ในหน้า Customer Dashboard ลูกค้าสามารถหยิบสินค้าใส่ตะกร้าได้และ `cart.js` จะบันทึกลง LocalStorage/DB แต่ในหน้า Fitting Room (`views/customer/fitting-room.ejs`) ไม่ได้มีการแนบ Script ควบคุมตะกร้า (Cart initialization) ไปด้วยอย่างสมบูรณ์ หรือไม่ได้เรียกใช้งาน Navbar ที่อัปเดต Badge จำนวนตะกร้า ทำให้ตัวเลขยังเป็น 0

## 5. แคตตาล็อกเสื้อผ้าทั้งหมดหายไปเมื่อรีเฟรชในหน้าห้องลอง
- **สาเหตุ:** Route `GET /customer/fitting-room` มีการ Query เอา `products` จาก `SELECT * FROM ITEM` ส่งไปให้ Template ทำการ render แต่ถ้าเกิดจังหวะที่หน้าเว็บ Refresh และ Session ไม่ตรงกัน ทำให้โค้ดรันไปตกในบล็อก Catch (เช่น Query ผิดพลาด หรือ Database lock) ระบบใช้การ `res.render('customer/fitting-room', { user: req.user, roomId, products: [] });` แทน ส่งผลให้แคตตาล็อกกลายเป็น Array ว่างเปล่า

## 6. หน้าชำระเงินไม่แสดงสินค้าเดิมก่อนเข้าห้องลอง
- **สาเหตุ:** ในฟังก์ชัน `exitAndAddToCart(goToCheckout = true)` ในห้องลอง ระบบได้ทำการทำ loop `await Cart.addItem(...)` เพื่อเพิ่มสินค้าที่ลองใส่ตะกร้า ทันทีที่คำสั่งจบ มันจะเปลี่ยนหน้าไป `/customer/checkout` ทันที ปัญหาคือ `cart.js` มีฟังก์ชัน `init()` ทำงานตอนเปิดหน้าใหม่ ซึ่งจะ fetch ข้อมูลตะกร้าเดิมจาก API Backend มาทับ LocalStorage ของลูกค้า ทำให้สินค้าที่เพิ่งเพิ่มลงไปแบบ Client-side สดๆ ร้อนๆ ที่ยัง sync ไปยัง Backend ไม่เสร็จ ถูกลบหายไปจากตะกร้า
